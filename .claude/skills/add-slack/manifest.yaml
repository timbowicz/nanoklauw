skill: slack
version: 2.0.0
description: "Slack Socket Mode integration with auto-registration, ephemeral status, /abort, and pluggable channel registry"
core_version: 0.1.0
adds:
  - src/channels/slack.ts
  - src/channels/slack.test.ts
modifies:
  - src/channels/index.ts
structured:
  npm_dependencies:
    "@slack/bolt": "^4.6.0"
  env_additions:
    - SLACK_BOT_TOKEN
    - SLACK_APP_TOKEN
    - SLACK_SIGNING_SECRET
conflicts: []
depends: []
test: "npx vitest run src/channels/slack.test.ts"
