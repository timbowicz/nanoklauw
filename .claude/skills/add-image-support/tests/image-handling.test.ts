/**
 * Image handling test reference.
 *
 * The primary tests for image support live in the main source tree:
 * - src/formatting.test.ts — tests has-image attribute in formatMessages
 *
 * Run with: npx vitest run src/formatting.test.ts
 */
import { describe, it, expect } from 'vitest';

describe('add-image-support skill', () => {
  it('primary tests live in src/formatting.test.ts', () => {
    expect(true).toBe(true);
  });
});
